# 4MB single-OTA partition layout for the T-Display (and other 4MB boards).
#
# One ~3MB factory app slot. This trades away A/B OTA rollback (a bad image
# needs a USB re-flash) for the headroom to fit any build -- including the
# larger WiFi-standalone firmware -- on a 4MB part.
#
# Dual-OTA on 4MB was measured out, not just deferred: the largest possible
# A/B slot is 0x1E0000 (1,966,080 B) and the v0.14.0 tdisplay app.bin is
# already 1,945,136 B -- 98.9% full, one feature away from unflashable. Field
# updates on 4MB boards therefore go through a host-side app-only re-flash
# (bootloader, partition table, NVS and config untouched), not OTA.
#
# Keeps the `nvs` namespace (master keys, policies, slots) and the `config`
# data partition the firmware seeds its network config from at boot.
# Name,      Type, SubType,  Offset,    Size,     Flags
nvs,         data, nvs,      0x9000,    0x6000,
phy_init,    data, phy,      0xf000,    0x1000,
factory,     app,  factory,  0x10000,   0x300000,
config,      data, 0x40,     0x310000,  0x4000,
